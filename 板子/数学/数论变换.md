$$
(f*g)(n)=\sum_{d\mid n}f(d)g\left(\frac nd\right)  
=\sum_{ab=n}f(a)g(b)  
$$

$$
f*\varepsilon=f  
$$

$$
\mathbf 1*\mu=\varepsilon  
$$

$$
\sum_{d\mid n}\mu(d)=[n=1]  
$$

$$
\varphi*\mathbf 1=\operatorname{id}  
$$

$$
\sum_{d\mid n}\varphi(d)=n  
$$

$$
\varphi=\operatorname{id}*\mu  
$$

$$
\varphi(n)=\sum_{d\mid n}d\mu\left(\frac nd\right)  
$$

$$
\varphi(n)=n\sum_{d\mid n}\frac{\mu(d)}d  
$$

$$
\frac{\varphi(n)}n=\sum_{d\mid n}\frac{\mu(d)}d  
$$

$$
[\gcd(x,y)=1]

\sum_{d\mid \gcd(x,y)}\mu(d)  
$$

$$
[\gcd(x,y)=1]

\sum_{\substack{d\mid x\\ d\mid y}}\mu(d)  
$$

$$
\gcd(a,b)

\sum_{d\mid \gcd(a,b)}\varphi(d)  
$$

$$
\gcd(a,b)

\sum_{\substack{d\mid a\\ d\mid b}}\varphi(d)  
$$

$$
\sum_{i=1}^{n}\gcd(i,n)

\sum_{d\mid n}d\varphi\left(\frac nd\right)  
$$

$$
\sum_{i=1}^{n}\gcd(i,n)

\sum_{d\mid n}\frac nd\varphi(d)  
$$

$$
\sum_{i=1}^{n}\gcd(i,n)

n\sum_{d\mid n}\frac{\varphi(d)}d  
$$

$$
\sum_{i=1}^{n}\sum_{j=1}^{m}[\gcd(i,j)=1]

\sum_{d=1}^{\min(n,m)}  
\mu(d)  
\left\lfloor\frac nd\right\rfloor  
\left\lfloor\frac md\right\rfloor  
$$

$$
\sum_{i=1}^{n}\sum_{j=1}^{m}\gcd(i,j)

\sum_{d=1}^{\min(n,m)}  
\varphi(d)  
\left\lfloor\frac nd\right\rfloor  
\left\lfloor\frac md\right\rfloor  
$$

$$
\sum_{i=1}^{n}\sum_{j=1}^{m}ij[\gcd(i,j)=1]

\sum_{d=1}^{\min(n,m)}  
\mu(d)d^2  
\left(\sum_{i=1}^{\lfloor n/d\rfloor}i\right)  
\left(\sum_{j=1}^{\lfloor m/d\rfloor}j\right)  
$$

$$
\sum_{i=1}^{k}i=\frac{k(k+1)}2  
$$

$$
\sum_{\substack{1\le i\le n\\ 1\le j\le m}}  
ij[\gcd(i,j)=d]

d^2  
\sum_{\substack{1\le x\le n/d\\ 1\le y\le m/d}}  
xy[\gcd(x,y)=1]  
$$

$$
f(n)=\sum_{d\mid n}g(d)  
\quad\Longleftrightarrow\quad  
g(n)=\sum_{d\mid n}\mu(d)f\left(\frac nd\right)  
$$

$$
f=g*\mathbf1  
\quad\Longleftrightarrow\quad  
g=f*\mu  
$$

$$
F(n)=\sum_{d\mid n}f(d)  
\quad\Longrightarrow\quad  
f(n)=\sum_{d\mid n}\mu(d)F\left(\frac nd\right)  
$$

$$
\sum_{d\mid n}d\mu\left(\frac nd\right)=\varphi(n)  
$$

$$
\sum_{d\mid n}\mu(d)\frac nd=\varphi(n)  
$$

$$
\operatorname{id}*\mu=\varphi  
$$

$$
\operatorname{id}=\varphi*\mathbf1  
$$

$$
(\operatorname{id}\cdot\mu)*\operatorname{id}  
\quad\text{等一类按约数展开为}\quad  
\sum_{d\mid n}d\mu(d)\frac nd  
$$

$$
\sum_{\substack{i,j\le n\ \gcd(i,j)\text{ 为质数}}}1

\sum_{p\le n}  
\sum_{\substack{i,j\le n\\ \gcd(i,j)=p}}1  
$$

$$

\sum_{p\le n}  
\sum_{\substack{x,y\le n/p\\ \gcd(x,y)=1}}1  
$$

$$
\sum_{\substack{1\le x,y\le N\\ \gcd(x,y)=1}}1

2\sum_{i=1}^{N}\varphi(i)-1  
$$

$$
\sum_{\substack{1\le x<y\le N\\ \gcd(x,y)=1}}1

\sum_{i=2}^{N}\varphi(i)  
$$

$$
\sum_{\substack{1\le x\le y\le N\\ \gcd(x,y)=1}}1

\sum_{i=1}^{N}\varphi(i)  
$$

$$
\sum_{d=1}^{n}f(d)\left\lfloor\frac nd\right\rfloor

\sum_{i=1}^{n}\sum_{d\mid i}f(d)  
$$

$$
\sum_{i=1}^{n}\sum_{d\mid i}f(d)

\sum_{d=1}^{n}f(d)\left\lfloor\frac nd\right\rfloor  
$$

$$
\left\lfloor\frac ni\right\rfloor

\left\lfloor\frac nj\right\rfloor  
\qquad  
i\le j\le  
\left\lfloor\frac{n}{\lfloor n/i\rfloor}\right\rfloor  
$$

$$
r=  
\left\lfloor  
\frac{n}{\lfloor n/l\rfloor}  
\right\rfloor  
$$