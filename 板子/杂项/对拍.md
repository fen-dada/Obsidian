```cpp
#include <iostream>
#include <cstdlib>
#include <string>
using namespace std;
int main() {
    int t = 0; 
    while (true) {
        t++;
        system("gen.exe > data.in");
        system("sol.exe < data.in > sol.out");
        system("bf.exe < data.in > bf.out");
        /*
	        system("./gen > data.in"); 
	        system("./sol < data.in > sol.out");
	        system("./bf < data.in > bf.out");
        */
        // if (system("diff -q sol.out bf.out > /dev/null"))
        if (system("fc sol.out bf.out > nul")) {
            break;
        } else {
            cout << "Test #" << t << ": AC" << endl;
        }
    }
    system("pause"); 
    return 0;
}
```